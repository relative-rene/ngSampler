import { Routes } from "@angular/router";
import {TDDComponent } from "./app.component";

export const tddRoutes: Routes = [
    {
      path: '', component: TDDComponent, children: [
      ]
    }
  ];
  

  